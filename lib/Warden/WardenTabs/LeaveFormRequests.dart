import 'package:flutter/material.dart';


class LeaveFormRequests extends StatefulWidget {
  const LeaveFormRequests({super.key});

  @override
  State<LeaveFormRequests> createState() => _LeaveFormRequestsState();
}

class _LeaveFormRequestsState extends State<LeaveFormRequests> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: const Text("Leave Form Requests"),
      ),

      body: Column(
        children: [

        ],
      ),
    );
  }
}